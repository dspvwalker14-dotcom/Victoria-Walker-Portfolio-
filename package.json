{
  "name": "azure-guest-to-member-workflow",
  "version": "1.0.0",
  "description": "Jira-triggered Azure AD workflow to preserve contractor access when converting a guest to a member.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.6.2"
  }
}
